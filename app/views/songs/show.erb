<h1>
  <%= @song.id %> | <%= @song.name %>
  <%= @song.artist.id %> | <%= @song.artist.name %>
  <a href = "/artists/<%= @song.artist.slug %>"> <%= @song.artist.name %></a>
  <ul>
    <% @song.genres.each do |genre| %>
      <li> <a href = "/genres/<%= genre.slug %>"> <%= genre.name %></a> </li>
      <% end %>
  </ul>
</h1>
